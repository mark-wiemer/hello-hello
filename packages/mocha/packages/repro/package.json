{
  "scripts": {
    "test:debug": "npx cross-env DEBUG=* npm test",
    "test": "mocha --parallel index.test.js --reporter json --watch --no-color --no-bail"
  },
  "dependencies": {
    "mocha": "file:../mocha"
  },
  "devDependencies": {
    "chai": "^6.2.2",
    "cross-env": "^10.1.0"
  }
}
