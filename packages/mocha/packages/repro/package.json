{
  "type": "module",
  "//": {
    "1": "this prop is just for notes, not used anywhere",
    "dependencies": {
      "mocha-local": "file:../mocha",
      "mocha-v11-stable": "11.8.0",
      "mocha-v12": "12.0.1"
    },
    "scripts": {
      "test:debug": "npx cross-env DEBUG=mocha:* npm test"
    }
  },
  "scripts": {
    "test:debug": "npx cross-env DEBUG=mocha:cli:mocha npm test",
    "test": "mocha --parallel --jobs 2 --reporter json; echo; echo Exit code $?; echo Mocha $(mocha --version); echo Node $(node --version)"
  },
  "devDependencies": {
    "cross-env": "10.1.0",
    "mocha": "12.0.1"
  }
}
