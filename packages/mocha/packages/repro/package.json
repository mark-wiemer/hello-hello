{
  "type": "module",
  "//": {
    "": "this is just for notes, not used anywhere",
    "dependencies": {
      "mocha-local": "file:../mocha",
      "mocha-registry": "11.7.5"
    }
  },
  "scripts": {
    "cli": "npx cross-env DEBUG=mocha:cli* mocha --no-package",
    "test:debug": "npx cross-env DEBUG=mocha:cli:mocha npm test",
    "test": "cross-env NODE_ENV=test mocha",
    "postinstall": "node install-custom-reporter.js"
  },
  "dependencies": {
    "mocha": "12.0.0-beta-9.2"
  },
  "devDependencies": {
    "cross-env": "10.1.0"
  }
}
