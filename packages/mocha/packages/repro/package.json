{
  "type": "module",
  "//": {
    "1": "this prop is just for notes, not used anywhere",
    "dependencies": {
      "mocha-local": "file:../mocha",
      "mocha-v11-stable": "11.8.0",
      "mocha-v12-beta": "12.0.0-beta-9.6",
      "mocha-v12-rc": "12.0.0-rc.6"
    },
    "scripts": {
      "test:debug": "npx cross-env DEBUG=mocha:* npm test"
    }
  },
  "scripts": {
    "test:debug": "npx cross-env DEBUG=mocha:cli:mocha npm test",
    "test": "npx cross-env MOCHA_OPTIONS=\"--grep 'foo bar' --color\" mocha; echo; echo Exit code $?; echo Mocha $(mocha --version); echo Node $(node --version)"
  },
  "dependencies": {
    "mocha": "12.0.0-rc.6"
  },
  "devDependencies": {
    "cross-env": "10.1.0",
    "semver": "7.7.4"
  }
}
