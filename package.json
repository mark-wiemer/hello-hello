{
  "author": "Mark Wiemer",
  "workspaces": [
    "packages/astro",
    "packages/github/pages",
    "packages/luanti/doc-schema",
    "packages/unified"
  ],
  "scripts": {
    "validate:fix": "prettier --write . && sort-package-json"
  },
  "prettier": {
    "printWidth": 100
  },
  "devDependencies": {
    "prettier": "3.6.2",
    "sort-package-json": "^3.4.0"
  },
  "engines": {
    "bun": "^1"
  }
}
