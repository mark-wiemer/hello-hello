{
  "scripts": {
    "postinstall": "pnpm run validate:fix",
    "validate:fix": "prettier --write . && sort-package-json"
  },
  "prettier": {
    "printWidth": 100
  },
  "devDependencies": {
    "prettier": "3.6.2",
    "sort-package-json": "^3.4.0"
  },
  "engines": {
    "node": "^22.21.1",
    "pnpm": "^10.23.0"
  }
}
